---
quick-task: 260615-h24
slug: swap-anthropic-client-usage-from-raw-ant
subsystem: agent-shared
tags: [langchain-anthropic, langchain-core, ChatAnthropic, claude, tdd]

# Dependency graph
requires:
  - phase: 01-infrastructure-and-observers
    provides: agent/shared/diagnoser_base.py, agent/shared/agent_loop.py, node_diagnoser, node_agent, registry-driven run_local.py
provides:
  - diagnose_with_claude and run_tool_loop rebuilt on langchain_anthropic.ChatAnthropic
  - node_diagnoser.diagnose / node_agent.run_incident / run_local.py consuming ChatAnthropic
  - tests/test_diagnoser_base.py and tests/test_agent_loop.py (10 new tests mocking ChatAnthropic)
affects: [phase-02-diagnosis-and-slo-engine, phase-03-safe-remediation]

# Tech tracking
tech-stack:
  added: [langchain-anthropic>=1.4.6, langchain-core>=1.4.7]
  patterns:
    - "diagnose_with_claude uses client.with_structured_output(schema, include_raw=True) + SystemMessage/HumanMessage"
    - "run_tool_loop uses client.bind_tools(tools) once + AIMessage.tool_calls / ToolMessage per iteration"
    - "model/max_tokens are bound on ChatAnthropic at construction time by the caller; diagnose_with_claude/run_tool_loop accept-and-ignore those params for signature compatibility"

key-files:
  created:
    - tests/test_diagnoser_base.py
    - tests/test_agent_loop.py
  modified:
    - agent/shared/diagnoser_base.py
    - agent/shared/agent_loop.py
    - agent/skills/diagnosers/node_diagnoser.py
    - agent/skills/agents/node_agent.py
    - agent/orchestrators/generic_orchestrator.py
    - scripts/run_local.py
    - requirements-monitor.txt

key-decisions:
  - "Reality-checked the task brief's '~10 test files mock Anthropic' claim against the actual tree (8 files / 72 tests, none mocking Anthropic) -- proceeded with the plan's corrected approach of adding 2 new RED-then-GREEN test files."
  - "tool_name/tool_description kept as accepted-but-unused kwargs on diagnose_with_claude for call-site compatibility (node_diagnoser's tool_description= call unchanged); tool_name still used in the RuntimeError message."
  - "model/max_tokens on diagnose_with_claude and run_tool_loop are accept-and-ignore -- callers construct ChatAnthropic(model=..., max_tokens=...) directly (scripts/run_local.py does this explicitly)."
  - "Removed anthropic>=0.40.0 from requirements-monitor.txt after confirming (grep) zero remaining direct imports of the raw anthropic package."
  - "generic_orchestrator.py AsyncAnthropic type hints updated to ChatAnthropic (type-hint-only, no behavioral change) per Task 3 step 4."

patterns-established:
  - "Pattern: forced single-tool structured output via with_structured_output(schema_dict, include_raw=True) -> {raw, parsed, parsing_error}; raise RuntimeError if parsing_error or parsed is None."
  - "Pattern: agentic tool loop appends AIMessage responses and ToolMessage results directly to a list[BaseMessage] -- no separate tool_results batching step."

requirements-completed: []

# Metrics
duration: ~12min
completed: 2026-06-15
---

# Quick Task 260615-h24: Swap Anthropic client usage from raw SDK to ChatAnthropic Summary

**diagnoser_base.diagnose_with_claude and agent_loop.run_tool_loop rebuilt on langchain_anthropic.ChatAnthropic (with_structured_output + bind_tools), with 10 new tests mocking the new shape; full suite green at 82 passed.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-15T19:17:00Z
- **Completed:** 2026-06-15T19:29:07Z
- **Tasks:** 3
- **Files modified:** 8 (2 created, 6 modified)

## Accomplishments
- `diagnose_with_claude` now calls `client.with_structured_output(schema, include_raw=True)` with `SystemMessage`/`HumanMessage`, preserving `cache_control: {"type": "ephemeral"}` on the system prompt and the `diagnoser_tokens`/`diagnoser_complete` log events with identical field names (`cache_read`, `cache_write`, `input_tokens`, `output_tokens`, `confidence`, `action_type`, `blast_radius`).
- `run_tool_loop` now calls `client.bind_tools(tools)` once and iterates `AIMessage.tool_calls` / appends `ToolMessage`s, preserving `finish_incident` handling, non-finish tool dispatch via `dispatch_tool`, `MAX_ITERATIONS` -> `human_escalator.escalate` auto-escalation, and `learning_mode` threading.
- `node_diagnoser.diagnose`, `node_agent.run_incident`, `generic_orchestrator.py`, and `scripts/run_local.py` updated to construct/type-annotate `ChatAnthropic` instead of `anthropic.AsyncAnthropic`; `scripts/run_local.py` constructs `ChatAnthropic(model="claude-sonnet-4-6", max_tokens=2048, api_key=api_key)` with the `ANTHROPIC_API_KEY` pre-flight guard preserved exactly.
- Added `tests/test_diagnoser_base.py` (5 tests) and `tests/test_agent_loop.py` (5 tests) mocking the `ChatAnthropic` shapes directly -- confirmed RED against the pre-swap implementation (TypeError on `client.messages.create` being a non-awaitable `MagicMock`), then GREEN after the rewrite.
- `requirements-monitor.txt`: added `langchain-anthropic>=1.4.6` and `langchain-core>=1.4.7` (both installed in `.venv`), removed `anthropic>=0.40.0` after confirming no remaining direct imports.
- Full suite: `.venv/bin/python -m pytest tests/ -q` -> 82 passed, 0 failed (72 baseline + 10 new).

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — add failing tests for ChatAnthropic-shaped diagnoser_base and agent_loop** - `b13de10` (test)
2. **Task 2: GREEN — rewrite diagnoser_base.diagnose_with_claude and agent_loop.run_tool_loop on ChatAnthropic** - `bceeeaf` (feat)
3. **Task 3: Update node_diagnoser, node_agent, run_local.py consumers; finalize requirements; full suite green** - `e06262f` (feat)

_Note: Task 1 was a standalone RED commit (test-only); Task 2 is the GREEN implementation commit for the same TDD feature._

## Files Created/Modified
- `tests/test_diagnoser_base.py` - 5 new tests mocking `ChatAnthropic.with_structured_output(..., include_raw=True)`: parsed result, cache_control preservation, input_schema override, RuntimeError on parsing_error, diagnoser_tokens/diagnoser_complete logging
- `tests/test_agent_loop.py` - 5 new tests mocking `ChatAnthropic.bind_tools(...)` + `AIMessage.tool_calls`/`ToolMessage`: finish_incident, non-finish tool dispatch, max_iterations escalation, cache_control on system message, learning_mode threading
- `agent/shared/diagnoser_base.py` - `diagnose_with_claude` rewritten on `with_structured_output(schema, include_raw=True)`; same token/result logging field names
- `agent/shared/agent_loop.py` - `run_tool_loop` rewritten on `bind_tools(tools)` + `AIMessage.tool_calls`/`ToolMessage`; same finish/dispatch/max-iterations/escalation flow
- `agent/skills/diagnosers/node_diagnoser.py` - `from anthropic import AsyncAnthropic` -> `from langchain_anthropic import ChatAnthropic`; `diagnose(client: ChatAnthropic, ...)`
- `agent/skills/agents/node_agent.py` - same import/type swap; `run_incident(anthropic_client: ChatAnthropic, ...)`
- `agent/orchestrators/generic_orchestrator.py` - `AsyncAnthropic` type hints -> `ChatAnthropic` (type-hint-only)
- `scripts/run_local.py` - `import anthropic` -> `from langchain_anthropic import ChatAnthropic`; constructs `ChatAnthropic(model="claude-sonnet-4-6", max_tokens=2048, api_key=api_key)`, `ANTHROPIC_API_KEY` guard preserved
- `requirements-monitor.txt` - added `langchain-anthropic>=1.4.6`, `langchain-core>=1.4.7`; removed `anthropic>=0.40.0`

## Decisions Made
- The task brief's claim of "~10 test files mock Anthropic" did not match reality (8 files / 72 tests, 0 mocking Anthropic). Followed the plan's corrected approach: write 2 new RED tests files first against the new `ChatAnthropic` shape, confirm they fail against the current raw-`anthropic` implementation, then implement the swap to GREEN.
- `model`/`max_tokens`/`tool_name`/`tool_description` parameters retained on `diagnose_with_claude` and `model` retained on `run_tool_loop` as accept-and-ignore kwargs for call-site signature compatibility (per plan's explicit guidance to avoid speculative `.bind()` usage where forwarding semantics were uncertain).
- `langchain-core` pinned as a direct dependency in `requirements-monitor.txt` (not just transitive via `langchain-anthropic`) since `tests/` imports `langchain_core.messages` directly.

## Deviations from Plan

None - plan executed exactly as written. (One setup-only note below, not a deviation from the plan's task content.)

## Issues Encountered
- **Worktree setup gap (pre-Task-1):** This worktree's branch (`worktree-agent-a3f058db8a5c85fd5`) was created from an old base commit (`66ff25a`, "initial commit") that predates all of Phase 1's `agent/`/`tests/` source — `git ls-files` showed none of the files the plan references. Verified `66ff25a` is a strict ancestor of `jignesh` (`e981bc1`, the current Phase-1-complete tip) with zero divergent commits, then ran `git merge --ff-only jignesh` (a non-destructive fast-forward, no commits discarded) to bring the worktree up to date. Also created `.venv` (none existed in the worktree) and installed `requirements-monitor.txt` + `requirements-dev.txt`, confirming the 72-test baseline before starting Task 1.
- Neither `langchain-anthropic` nor `langchain-core` were installed in the fresh `.venv`. Installed both (`langchain-anthropic==1.4.6`, `langchain-core==1.4.7`) as part of Task 1 setup, since Task 1's RED tests require `from langchain_core.messages import ...` to even be collectible. These are the exact packages named in the plan's `<interfaces>` section (not substitutions), and `requirements-monitor.txt` was updated in Task 2/3 to reflect the installed versions as `>=` floors.

## User Setup Required

None - no external service configuration required. (Real end-to-end runs of `scripts/run_local.py` still require `ANTHROPIC_API_KEY`, `minikube`, and `Redis` as documented in the script's existing prerequisites — unchanged by this task.)

## Next Phase Readiness
- `agent/shared/diagnoser_base.py` and `agent/shared/agent_loop.py` are now on `langchain_anthropic.ChatAnthropic`; future domains (Phase 2/3: EC2, Postgres) reuse these shared modules unchanged.
- `agent/shared/safety.py` untouched (byte-identical, frozen per CLAUDE.md) — verified via `git diff --stat agent/shared/safety.py` (empty).
- `agent/registry.py` and `agent/shared/remediation.py` unchanged — verified via `git diff --stat` (empty).
- No new tool/capability introduced; no delete/drop/destroy/terminate operations added.

---
*Quick task: 260615-h24*
*Completed: 2026-06-15*

## Self-Check: PASSED

All created/modified files exist; all 3 task commit hashes (b13de10, bceeeaf, e06262f) found in git log.
