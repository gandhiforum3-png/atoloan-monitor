---
phase: 01-infrastructure-and-observers
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - agent/registry.py
  - agent/orchestrators/generic_orchestrator.py
  - agent/orchestrators/k8s_orchestrator.py
  - agent/skills/remediators/human_escalator.py
  - tests/test_registry.py
  - tests/test_escalator.py
  - tests/test_orchestrator_routing.py
autonomous: true
requirements: [OBS-02, OBS-09]
user_setup: []

must_haves:
  truths:
    - "agent/registry.py defines DomainConfig and a REGISTRY dict; the k8s domain is registered"
    - "K8s DomainConfig.stream == 'events:k8s' and consumer_group == 'k8s-orchestrator' (runtime keys preserved)"
    - "generic_orchestrator.run(redis, anthropic_client, config, *, debounce_seconds, learning_mode, mode) drives the debounce/consumer-group loop for any DomainConfig"
    - "k8s_orchestrator.py shrinks to _fetch_pods_on_nodes + the k8s DomainConfig registration; the generic loop is NOT duplicated there"
    - "human_escalator._derive_urgency takes a per-domain urgency_map arg; the k8s map lives in the k8s DomainConfig.urgency_map; p3_within_1h fallback preserved"
    - "_should_escalate uses config.escalate_below; routing behavior identical to current"
    - "All Wave 0 baseline tests still pass plus a new registry test"
  artifacts:
    - path: "agent/registry.py"
      provides: "DomainConfig dataclass + REGISTRY + register(); single wiring point for domains"
      contains: "class DomainConfig"
    - path: "agent/orchestrators/generic_orchestrator.py"
      provides: "domain-agnostic consume/debounce/bundle/dispatch loop"
      contains: "async def run"
    - path: "agent/orchestrators/k8s_orchestrator.py"
      provides: "k8s _fetch_pods_on_nodes context fetcher + k8s DomainConfig registration"
      contains: "_fetch_pods_on_nodes"
    - path: "tests/test_registry.py"
      provides: "registry construction + k8s wiring assertions"
      min_lines: 25
  key_links:
    - from: "agent/orchestrators/k8s_orchestrator.py"
      to: "agent/registry.py"
      via: "register(DomainConfig(domain='k8s', ...))"
      pattern: "DomainConfig\\("
    - from: "agent/orchestrators/generic_orchestrator.py"
      to: "agent/registry.py"
      via: "config: DomainConfig parameter"
      pattern: "DomainConfig"
    - from: "agent/skills/remediators/human_escalator.py"
      to: "DomainConfig.urgency_map"
      via: "_derive_urgency(signals, urgency_map)"
      pattern: "def _derive_urgency"
---

<objective>
Build the domain registry and generic orchestrator (D-10/D-11/D-12) and make
urgency per-domain (D-13). Extract the consume/debounce/bundle/dispatch loop out
of k8s_orchestrator into generic_orchestrator.run(config: DomainConfig); shrink
k8s_orchestrator to its K8s context fetcher + the single k8s DomainConfig
registration. Make _derive_urgency take the map as an arg; move the K8s map into
the registry; keep the p3 fallback.

This is the heart of the framework: one DomainConfig per domain becomes the only
place a new monitor wires in. K8s behavior (routing, urgency incl. fall-throughs)
identical.

Purpose: Adding a domain = one DomainConfig entry, no orchestrator copy-paste.
Output: agent/registry.py, generic_orchestrator.py, shrunk k8s_orchestrator.py,
per-domain human_escalator.

Requirement honesty note: This is the framework groundwork that makes deferred
OBS-03..08 mechanical. Preserves OBS-02 routing/urgency and OBS-09 (InfraEvent
domain derivation now from config.domain, schema unchanged). Delivers nothing new
for the deferred IDs.
</objective>

<execution_context>
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-and-observers/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-observers/01-RESEARCH.md

<interfaces>
<!-- Exact current shapes. From k8s_orchestrator.py + human_escalator.py (post 01-03). -->

CURRENT k8s_orchestrator.py module-level constants + functions (to generalize):
```python
STREAM = "events:k8s"; CONSUMER_GROUP = "k8s-orchestrator"; CONSUMER_NAME = "atoloan-monitor-1"
_ESCALATE_BELOW = {"pod_restart":0.80,"deployment_scale_down":0.85,"human_escalate":1.1,"observe_only":1.1}
def _should_escalate(diagnosis) -> tuple[bool,str]   # uses _ESCALATE_BELOW.get(action, 0.80)
def _decode_event(msg_id, fields) -> InfraEvent      # hardcodes domain="k8s" (line 74)
async def _fetch_pods_on_nodes(node_names: set[str]) -> list[dict]   # K8s-specific, STAYS
async def _handle_bundle(redis, anthropic_client, bundle, *, learning_mode, mode)
async def run(redis, anthropic_client, *, debounce_seconds=30, learning_mode=True, mode="diagnoser")
# run(): xgroup_create; XREADGROUP loop; debounce window; SignalBundle(domain="k8s",...) line 247;
#        asyncio.create_task(_handle_bundle(...))
# _handle_bundle: mode=="agent" -> node_agent.run_incident; else _fetch_pods_on_nodes ->
#   node_diagnoser.diagnose -> _should_escalate -> human_escalator.escalate OR node_remediator.remediate
```

CURRENT human_escalator.py (post 01-03 — unchanged from original):
```python
_URGENCY_MAP = {"node_not_ready":"p1_immediate","node_memory_pressure":"p2_within_15m",
                "node_disk_pressure":"p2_within_15m","node_pid_pressure":"p2_within_15m"}
def _derive_urgency(signals) -> str   # p1 if any node_not_ready; else first mapped; else "p3_within_1h"
async def escalate(redis, domain, diagnosis, signals, why) -> str   # builds HumanEscalationPacket
# escalate summary string currently: f"{len(signals)} node signal(s) detected. ..."  (line 67)
```

Available callables to reference from DomainConfig:
- node_diagnoser.diagnose(client, bundle, pods_on_node)
- node_remediator.remediate(redis, *, incident_id, diagnosis, signals, learning_mode)
- node_agent.run_incident(anthropic_client, redis, bundle, incident_id, *, learning_mode)
- _fetch_pods_on_nodes(node_names) -> list[dict]
- watch_nodes(redis, *, verbose=False)   # observer entrypoint (D-14 needs it; add observer field)

DESIGN NOTE (research): _ESCALATE_BELOW (routing) and THRESHOLDS["k8s"] (execution gate)
are TWO separate dicts with intentionally different sentinels (1.1 vs 0.00). DO NOT merge.
escalate_below in DomainConfig = the _ESCALATE_BELOW map.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent/registry.py (DomainConfig + REGISTRY) and per-domain urgency in human_escalator</name>
  <read_first>
    - agent/skills/remediators/human_escalator.py (full file — _URGENCY_MAP, _derive_urgency, escalate)
    - agent/orchestrators/k8s_orchestrator.py (_ESCALATE_BELOW for escalate_below; constants for stream/group)
    - .planning/phases/01-infrastructure-and-observers/01-RESEARCH.md (DomainConfig Design Considerations section)
    - tests/test_escalator.py (baseline urgency test — update to pass the map explicitly)
  </read_first>
  <behavior>
    - `agent.registry` imports cleanly; `DomainConfig` is a dataclass; `REGISTRY` is a dict; `register(cfg)` populates `REGISTRY[cfg.domain]`
    - `_derive_urgency(signals, urgency_map)` returns p1_immediate for node_not_ready, p2_within_15m for the pressure events, and p3_within_1h for node_network_unavailable / node_deleted / unmapped / empty (fall-through preserved)
    - The k8s urgency map equals the original 4-entry map exactly (no new entries)
  </behavior>
  <action>
    Create `agent/registry.py` defining `@dataclass class DomainConfig` with fields
    (per D-10 + the research-flagged observer field A2):
    `domain: str`, `stream: str`, `consumer_group: str`,
    `observer: Callable[..., Awaitable[None]]`, `diagnose: Callable[..., Awaitable]`,
    `remediate: Callable[..., Awaitable]`, `run_incident: Callable[..., Awaitable]`,
    `escalate_below: dict[str, float]`, `urgency_map: dict[str, str]`,
    `context_fetcher: Optional[Callable[..., Awaitable[list[dict]]]] = None`.
    Add `REGISTRY: dict[str, DomainConfig] = {}` and
    `def register(cfg: DomainConfig) -> None: REGISTRY[cfg.domain] = cfg`.
    Imports: dataclasses dataclass, typing Awaitable/Callable/Optional. Do NOT import
    any agent.skills modules here (registry is consumed BY k8s_orchestrator, not the
    reverse — avoids a circular import). NO kubernetes_asyncio.

    Modify `agent/skills/remediators/human_escalator.py`:
    - Change `_derive_urgency(signals)` to `_derive_urgency(signals: list[InfraEvent],
      urgency_map: dict[str, str]) -> str` — replace the two `_URGENCY_MAP.get(...)`
      lookups with `urgency_map.get(...)`. Keep the exact algorithm: first pass returns
      "p1_immediate" if any event maps to it; second pass returns the first mapped
      urgency; else `return "p3_within_1h"`.
    - DELETE the module-level `_URGENCY_MAP` (it moves into the k8s DomainConfig in Task 2).
    - Change `escalate(...)` to accept `urgency_map: dict[str, str] | None = None` and
      pass it to `_derive_urgency(signals, urgency_map or {})`. (Defaulting to `{}`
      means an empty map -> everything falls through to p3_within_1h, which is the safe
      conservative default for any caller that doesn't supply one.)
    - Genericize the summary string from "node signal(s)" to
      `f"{len(signals)} {domain} signal(s) detected. Root cause: {diagnosis.root_cause[:120]}"`
      (this is the one intentional cosmetic behavior change called out in research
      Open Question 3 — note it in the SUMMARY so verify-work does not flag it).

    Update `tests/test_escalator.py`: import `_derive_urgency` from human_escalator and
    define the k8s map inline in the test (the same 4 entries), passing it as the second
    arg. Keep ALL existing assertions including the node_network_unavailable / node_deleted
    -> p3_within_1h fall-throughs and the empty-list -> p3_within_1h case. Add one new
    assertion: `_derive_urgency([make_event(event_type="node_not_ready")], {}) ==
    "p3_within_1h"` (empty map -> fall-through, proving the default-safe behavior).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import agent.registry, agent.skills.remediators.human_escalator" && .venv/bin/python -m pytest tests/test_escalator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent.registry"` exits 0
    - `grep -q "class DomainConfig" agent/registry.py` exits 0
    - `grep -q "REGISTRY" agent/registry.py` exits 0
    - `grep -q "observer:" agent/registry.py` exits 0 (observer field present for D-14)
    - `grep -q "context_fetcher" agent/registry.py` exits 0
    - `grep -L kubernetes_asyncio agent/registry.py` prints the filename
    - `grep -Eq "def _derive_urgency\(signals.*urgency_map" agent/skills/remediators/human_escalator.py` exits 0
    - `grep -cE "^_URGENCY_MAP" agent/skills/remediators/human_escalator.py` returns 0 (module-level map removed)
    - `grep -q "p3_within_1h" agent/skills/remediators/human_escalator.py` exits 0 (fallback kept)
    - `.venv/bin/python -m pytest tests/test_escalator.py -q` exits 0
  </acceptance_criteria>
  <done>registry.py defines DomainConfig+REGISTRY+register (no skill imports, K8s-free); _derive_urgency takes the map as an arg with p3 fallback; module-level _URGENCY_MAP removed; escalator summary genericized; urgency baseline (incl. fall-throughs) green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract generic_orchestrator.run and shrink k8s_orchestrator to fetcher + registration</name>
  <read_first>
    - agent/orchestrators/k8s_orchestrator.py (full file — run/_handle_bundle/_decode_event move; _fetch_pods_on_nodes + registration stay)
    - agent/registry.py (DomainConfig — created in Task 1)
    - agent/skills/remediators/human_escalator.py (escalate now takes urgency_map — created in Task 1)
    - agent/skills/diagnosers/node_diagnoser.py, agent/skills/remediators/node_remediator.py, agent/skills/agents/node_agent.py (the callables wired into DomainConfig)
    - agent/observers/k8s_node_observer.py (watch_nodes — the observer entrypoint)
    - tests/test_registry.py, tests/test_orchestrator_routing.py
  </read_first>
  <behavior>
    - `generic_orchestrator.run` consumes from `config.stream` with `config.consumer_group`, derives event domain from `config.domain`, builds `SignalBundle(domain=config.domain,...)`, and dispatches via config.diagnose/remediate/run_incident/context_fetcher
    - `_should_escalate(diagnosis, escalate_below)` matches current routing for k8s exactly (human_escalate->escalate, requires_human_review->escalate, confidence < escalate_below.get(action,0.80)->escalate, else not)
    - The k8s DomainConfig has stream=="events:k8s", consumer_group=="k8s-orchestrator", urgency_map equal to the original 4-entry map, escalate_below equal to the original _ESCALATE_BELOW (incl. 1.1 sentinels), and observer/diagnose/remediate/run_incident/context_fetcher wired to the k8s callables
    - k8s_orchestrator.py does NOT contain the consume/debounce loop (no duplicate)
  </behavior>
  <action>
    Create `agent/orchestrators/generic_orchestrator.py` with:
    `async def run(redis: Redis, anthropic_client: AsyncAnthropic, config: DomainConfig,
    *, debounce_seconds: int = 30, learning_mode: bool = True,
    mode: Literal["diagnoser","agent"] = "diagnoser") -> None`. Move the current
    k8s_orchestrator `run`, `_handle_bundle`, `_decode_event`, and `_should_escalate`
    bodies here, parameterized by config:
    - `_decode_event` builds `InfraEvent(domain=config.domain, ...)` (was hardcoded "k8s").
      Pass config or domain into it.
    - xgroup_create / XREADGROUP use `config.stream` and `config.consumer_group`
      (CONSUMER_NAME may stay a module constant "atoloan-monitor-1").
    - `SignalBundle(domain=config.domain, ...)` (was "k8s").
    - `_should_escalate(diagnosis, config.escalate_below)` — change signature to take the
      map; `threshold = escalate_below.get(diagnosis.action_type, 0.80)`.
    - `_handle_bundle`: mode=="agent" -> `await config.run_incident(anthropic_client,
      redis, bundle, incident_id, learning_mode=learning_mode)`; else
      `pods = await config.context_fetcher(node_names) if config.context_fetcher else []`,
      `diagnosis = await config.diagnose(anthropic_client, bundle, pods)`, then
      `_should_escalate(diagnosis, config.escalate_below)` ->
      `human_escalator.escalate(redis, domain=config.domain, diagnosis=diagnosis,
      signals=bundle.signals, why=reason, urgency_map=config.urgency_map)` OR
      `config.remediate(redis, incident_id=incident_id, diagnosis=diagnosis,
      signals=bundle.signals, learning_mode=learning_mode)`.
    Imports: asyncio, json, uuid, datetime, typing Literal, structlog, anthropic
    AsyncAnthropic, redis.asyncio Redis, redis.exceptions ResponseError,
    `from agent.shared.models import DiagnosisResult, InfraEvent, SignalBundle`,
    `from agent.registry import DomainConfig`, `from agent.skills.remediators import
    human_escalator`. NO kubernetes_asyncio (the K8s context fetch is injected via config).

    Shrink `agent/orchestrators/k8s_orchestrator.py` to (D-12, keep-the-file option):
    - Keep `_fetch_pods_on_nodes(node_names)` exactly as-is (the only K8s-API piece).
    - Remove STREAM/CONSUMER_GROUP/_ESCALATE_BELOW/_should_escalate/_decode_event/
      _handle_bundle/run (all now generic).
    - Add the k8s DomainConfig registration: import `register, DomainConfig` from
      agent.registry; import watch_nodes, node_diagnoser.diagnose,
      node_remediator.remediate, node_agent.run_incident; then call
      `register(DomainConfig(domain="k8s", stream="events:k8s",
      consumer_group="k8s-orchestrator", observer=watch_nodes,
      diagnose=node_diagnoser.diagnose, remediate=node_remediator.remediate,
      run_incident=node_agent.run_incident, context_fetcher=_fetch_pods_on_nodes,
      escalate_below={"pod_restart":0.80,"deployment_scale_down":0.85,
      "human_escalate":1.1,"observe_only":1.1},
      urgency_map={"node_not_ready":"p1_immediate","node_memory_pressure":"p2_within_15m",
      "node_disk_pressure":"p2_within_15m","node_pid_pressure":"p2_within_15m"}))`
      at module import time. Keep the kubernetes_asyncio import (needed by _fetch_pods_on_nodes).

    Create `tests/test_registry.py`: `import agent.orchestrators.k8s_orchestrator` (triggers
    registration), then `from agent.registry import REGISTRY`; assert `"k8s" in REGISTRY`;
    `cfg = REGISTRY["k8s"]`; assert `cfg.stream == "events:k8s"`,
    `cfg.consumer_group == "k8s-orchestrator"`, `cfg.escalate_below["pod_restart"] == 0.80`,
    `cfg.escalate_below["human_escalate"] == 1.1`,
    `cfg.urgency_map["node_not_ready"] == "p1_immediate"`,
    `"node_network_unavailable" not in cfg.urgency_map` (preserves the intentional gap),
    and `callable(cfg.observer) and callable(cfg.diagnose) and callable(cfg.remediate)
    and callable(cfg.run_incident) and callable(cfg.context_fetcher)`.

    Update `tests/test_orchestrator_routing.py`: import `_should_escalate` from
    `agent.orchestrators.generic_orchestrator`; pass the escalate_below map (use
    `REGISTRY["k8s"].escalate_below`) as the second arg in every call. Keep ALL existing
    routing assertions unchanged in outcome.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import agent.orchestrators.generic_orchestrator, agent.orchestrators.k8s_orchestrator; from agent.registry import REGISTRY; assert 'k8s' in REGISTRY" && .venv/bin/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent.orchestrators.generic_orchestrator, agent.orchestrators.k8s_orchestrator"` exits 0
    - `grep -q "async def run" agent/orchestrators/generic_orchestrator.py` exits 0
    - `grep -q "config: DomainConfig" agent/orchestrators/generic_orchestrator.py` exits 0
    - `grep -q "config.stream" agent/orchestrators/generic_orchestrator.py` exits 0
    - `grep -q "domain=config.domain" agent/orchestrators/generic_orchestrator.py` exits 0
    - `grep -q "urgency_map=config.urgency_map" agent/orchestrators/generic_orchestrator.py` exits 0
    - `grep -L kubernetes_asyncio agent/orchestrators/generic_orchestrator.py` prints the filename (generic loop is K8s-free)
    - `grep -q "_fetch_pods_on_nodes" agent/orchestrators/k8s_orchestrator.py` exits 0 (fetcher stays)
    - `grep -q "register(DomainConfig" agent/orchestrators/k8s_orchestrator.py` exits 0
    - `grep -cE "for .* in \(entries or \[\]\)|xreadgroup" agent/orchestrators/k8s_orchestrator.py` returns 0 (loop removed, no duplicate)
    - `.venv/bin/python -m pytest tests/ -q` exits 0 (all baselines + registry test green)
  </acceptance_criteria>
  <done>generic_orchestrator.run drives the loop from DomainConfig (K8s-free); k8s_orchestrator holds only _fetch_pods_on_nodes + the k8s registration; REGISTRY["k8s"] has correct stream/group/escalate_below/urgency_map and wired callables; routing + registry tests green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis stream → orchestrator | untrusted event fields decoded into InfraEvent; domain now from config |
| diagnosis → routing | confidence/action_type cross the escalate/remediate split |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | merging escalate_below with THRESHOLDS | mitigate | Kept as two separate dicts in DomainConfig (escalate_below vs THRESHOLDS["k8s"]); test_registry asserts escalate_below has the 1.1 sentinels (distinct from THRESHOLDS 0.00). |
| T-01-10 | Tampering | urgency map "filled in" during move (Pitfall 3) | mitigate | k8s urgency_map copied verbatim (4 entries); test_registry asserts node_network_unavailable NOT in map; test_escalator asserts node_deleted/network_unavailable -> p3 fall-through. |
| T-01-11 | Spoofing | wrong stream/group strands Redis consumer offsets | mitigate | k8s DomainConfig.stream=="events:k8s", consumer_group=="k8s-orchestrator" asserted in test_registry — runtime keys unchanged. |
| T-01-12 | Elevation | kubernetes_asyncio leak into generic_orchestrator | mitigate | K8s context fetch injected via config.context_fetcher; grep -L gate on generic_orchestrator.py. |
</threat_model>

<verification>
- generic_orchestrator.run is config-driven and kubernetes_asyncio-free.
- k8s_orchestrator holds only the fetcher + registration; no duplicate loop.
- REGISTRY["k8s"] preserves runtime keys, both threshold dicts, and the exact urgency map.
- Full Wave 0 suite + registry test green.
</verification>

<success_criteria>
- D-10/D-11/D-12/D-13 complete.
- Routing + urgency behavior identical for k8s (baselines prove it).
- The two threshold dicts kept separate; the urgency-map gap preserved.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-and-observers/01-04-SUMMARY.md` when done.
</output>
