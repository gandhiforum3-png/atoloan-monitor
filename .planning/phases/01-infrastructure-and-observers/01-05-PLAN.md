---
phase: 01-infrastructure-and-observers
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - agent/shared/models.py
  - agent/skills/diagnosers/node_diagnoser.py
  - tests/test_models.py
  - tests/test_orchestrator_routing.py
  - tests/test_safety.py
autonomous: false
requirements: [OBS-02, OBS-09]
user_setup: []

must_haves:
  truths:
    - "DiagnosisResult.action_type is an open str (no Literal, no enum in the model JSON schema)"
    - "A DiagnosisResult with a novel action_type (e.g. 'reboot_node') validates but routes to escalation via the boundary (never executes)"
    - "FORBIDDEN_OPERATIONS still blocks delete/terminate/drop/drain/cordon regardless of the now-open action_type"
    - "learning_mode still blocks execution; the remediate() gate order (safety -> threshold -> learning_mode -> execute) is unchanged"
    - "node_diagnoser re-injects a per-domain enum into its own tool schema so its Claude call stays API-constrained; _SYSTEM_PROMPT prose still lists the 4 actions"
    - "All Wave 0 baseline tests pass with the updated model-schema expectations"
  artifacts:
    - path: "agent/shared/models.py"
      provides: "DiagnosisResult with open str action_type"
      contains: "action_type: str"
    - path: "tests/test_safety.py"
      provides: "regression proving the safety floor + learning_mode hold after action_type opens"
      min_lines: 20
  key_links:
    - from: "agent/shared/models.py DiagnosisResult"
      to: "node_diagnoser tool input_schema"
      via: "model_json_schema() loses enum; diagnoser re-injects per-domain enum"
      pattern: "action_type: str"
    - from: "novel action_type"
      to: "escalation"
      via: "_meets_threshold returns False -> _should_escalate escalates"
      pattern: "reboot_node"
---

<objective>
Open DiagnosisResult.action_type from the closed Literal to an open str
(D-01/D-02), and prove the safety/escalation boundary still contains the wider
output. This is the single highest-risk change in the phase: opening the field
removes the JSON-schema enum that previously API-constrained Claude to 4 actions.

Sequenced LAST among code changes (Wave 4) per 01-RESEARCH.md safe-ordering: all
extraction (shared modules, registry, generic orchestrator) is already done and
proven equivalent, so the only new variable here is the open field plus its
boundary defenses. agent/shared/safety.py is FROZEN and is NOT modified by this
plan — but because this plan exercises the safety floor under the new open field,
it carries a blocking human-verify checkpoint per the phase quality gate.

Purpose: Let future domains (EC2 reboot_instance, Postgres kill_query) express
valid actions the Literal forbids, without weakening the hard floor.
Output: open action_type + regression tests proving forbidden ops + learning_mode
+ unknown-action-escalation all still hold.

Requirement honesty note: D-01/D-02 are the structural prerequisite that makes the
deferred OBS-03..08 / REM-02..08 domains expressible. Preserves OBS-02 and OBS-09
(InfraEvent schema is NOT touched — only DiagnosisResult.action_type changes).
</objective>

<execution_context>
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-and-observers/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-observers/01-RESEARCH.md

<interfaces>
<!-- The exact change and its verified consequence. -->

CURRENT models.py (lines 48-50) — the change:
  action_type: Literal["pod_restart","deployment_scale_down","human_escalate","observe_only"] = Field(...)
  BECOMES:
  action_type: str = Field(description="Machine-readable action category")

VERIFIED (research, pydantic 2.13.4): with Literal, model_json_schema() gives
action_type {"enum":[...],"type":"string"}; with str it gives {"type":"string"}
(no enum). node_diagnoser.py:136 reuses DiagnosisResult.model_json_schema() as the
tool input_schema, so the API enum constraint disappears unless re-injected.

DO NOT TOUCH (D-01 scope is action_type ONLY):
- InfraEvent.domain (Literal["k8s","db","infra","app"]) — stays Literal
- InfraEvent.severity, HumanEscalationPacket.urgency, estimated_blast_radius — stay Literal
- agent/shared/safety.py — FROZEN, not modified by this plan

The boundary that already contains a novel action (from plans 02/04):
- _meets_threshold(domain, action, conf) returns False for unregistered actions (2-level default 1.0)
- _should_escalate(diagnosis, escalate_below): escalate_below.get(action, 0.80) default -> low/unknown escalates
- safety_check(operation, target) raises SafetyViolation for any FORBIDDEN_OPERATIONS member
- remediate() gate order: safety_check -> observe_only -> _meets_threshold -> learning_mode -> execute
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Open action_type to str and add boundary regression tests</name>
  <read_first>
    - agent/shared/models.py (lines 48-50 — the Literal to open; surrounding fields stay Literal)
    - agent/shared/safety.py (FROZEN — read to confirm the floor; do NOT edit)
    - agent/skills/diagnosers/node_diagnoser.py (_SYSTEM_PROMPT enumerates 4 actions; tool input_schema at ~136)
    - agent/orchestrators/generic_orchestrator.py (_should_escalate — the routing boundary)
    - agent/shared/remediation.py (_meets_threshold — the threshold boundary)
    - tests/test_models.py, tests/test_orchestrator_routing.py, tests/test_safety.py (baselines to update/extend)
  </read_first>
  <behavior>
    - DiagnosisResult(action_type="reboot_node", confidence=0.99, ...) constructs without error (open str)
    - DiagnosisResult.model_json_schema()["properties"]["action_type"] has NO "enum" key
    - confidence bounds still enforced (confidence=1.5 -> ValidationError)
    - _meets_threshold("k8s", "reboot_node", 0.99) is False (unknown action -> never meets)
    - _should_escalate(novel-action diagnosis, REGISTRY["k8s"].escalate_below) -> escalate True (never executes)
    - safety_check("drain_node","x") and safety_check("terminate_instance","x") still raise SafetyViolation
  </behavior>
  <action>
    In agent/shared/models.py: change DiagnosisResult.action_type from the 4-value
    Literal to `action_type: str = Field(description="Machine-readable action category")`.
    Change nothing else — InfraEvent.domain, severity, urgency, and estimated_blast_radius
    all stay Literal. Do not touch agent/shared/safety.py.

    In agent/skills/diagnosers/node_diagnoser.py (belt-and-suspenders per research Open
    Question 1; the boundary remains the real defense): the tool input_schema currently
    equals DiagnosisResult.model_json_schema(). Re-inject a per-domain enum so THIS
    domain's Claude call keeps the API constraint without re-closing the shared model.
    Build `schema = DiagnosisResult.model_json_schema()`, then set
    `schema["properties"]["action_type"]["enum"] = ["pod_restart","deployment_scale_down",
    "human_escalate","observe_only"]`, and pass that dict as the tool input_schema.
    Keep _SYSTEM_PROMPT's prose enumeration of the 4 actions unchanged (second layer).
    The agent path (node_agent TOOLS) already constrains via explicit per-tool schemas
    and is unaffected.

    Update tests/test_models.py: REPLACE the prior "schema has enum" assertion (which
    pinned CURRENT Literal behavior) with the post-D-01 expectation —
    assert "enum" NOT in DiagnosisResult.model_json_schema()["properties"]["action_type"].
    Add: DiagnosisResult(action_type="reboot_node", ...) constructs successfully;
    confidence=1.5 still raises ValidationError. Add a comment that this expectation was
    intentionally flipped by D-01.

    Update tests/test_orchestrator_routing.py: add the novel-action case now permitted —
    _should_escalate(make_diagnosis(action_type="reboot_node", confidence=0.99,
    requires_human_review=False), REGISTRY["k8s"].escalate_below) -> escalate True.
    Import REGISTRY from agent.registry and ensure agent.orchestrators.k8s_orchestrator
    is imported so the k8s domain is registered. Keep all prior routing assertions green.

    Extend tests/test_safety.py: add post-D-01 floor regressions — assert
    safety_check("drain_node","x"), safety_check("terminate_instance","x"), and
    safety_check("drop_table","x") each raise SafetyViolation (the open field does NOT
    let a forbidden op through). Add a _meets_threshold("k8s","reboot_node",0.99) is False
    assertion to pin "unknown action never executes" at the threshold layer. Add a comment
    that these prove FORBIDDEN_OPERATIONS and threshold gating are unaffected by opening
    action_type.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from agent.shared.models import DiagnosisResult as D; assert 'enum' not in D.model_json_schema()['properties']['action_type']; D(root_cause='x',affected_components=[],contributing_factors=[],confidence=0.9,confidence_reasoning='x',recommended_action='x',action_type='reboot_node',requires_human_review=False,estimated_blast_radius='service')"</automated>
    <automated>.venv/bin/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "action_type: str" agent/shared/models.py` exits 0
    - `grep -c "Literal\[" agent/shared/models.py` >= 3 (domain/severity/urgency/blast_radius Literals untouched)
    - `grep -c "pod_restart" agent/shared/models.py` returns 0 (no leftover action Literal values)
    - `.venv/bin/python -c "from agent.shared.models import DiagnosisResult as D; assert 'enum' not in D.model_json_schema()['properties']['action_type']"` exits 0
    - `grep -q '"enum"' agent/skills/diagnosers/node_diagnoser.py` exits 0 (per-domain enum re-injected)
    - `grep -q "reboot_node" tests/test_orchestrator_routing.py` exits 0 (novel-action routing tested)
    - `grep -q "enum" tests/test_models.py` exits 0 (flipped to assert NO enum — string present in the assertion)
    - `grep -q "drain_node" tests/test_safety.py` exits 0
    - `git diff --quiet agent/shared/safety.py` exits 0 (safety.py byte-unchanged this plan)
    - `.venv/bin/python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>action_type is open str; model schema has no enum but diagnoser re-injects a per-domain enum; novel actions validate yet escalate (never execute); forbidden ops + learning_mode floor proven intact; safety.py byte-unchanged; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    DiagnosisResult.action_type opened from a closed 4-value Literal to an open str
    (D-01/D-02). The JSON-schema enum that previously API-constrained Claude is gone
    from the shared model; the node diagnoser re-injects a per-domain enum for its own
    tool call. The safety floor (agent/shared/safety.py) was NOT modified.

    Automated proof already captured in Task 1:
    - tests/test_safety.py: forbidden ops (drain_node, terminate_instance, drop_table)
      still raise SafetyViolation under the open field; _meets_threshold rejects unknown actions.
    - tests/test_orchestrator_routing.py: a novel action_type="reboot_node" routes to
      escalation, never to execution.
    - tests/test_models.py: model schema has no enum; novel action validates; confidence
      bounds still enforced.
    - git diff confirms agent/shared/safety.py is byte-unchanged.
  </what-built>
  <how-to-verify>
    This is the safety-critical gate of the phase. Confirm:
    1. Run `.venv/bin/python -m pytest tests/test_safety.py tests/test_models.py tests/test_orchestrator_routing.py -v`
       — all green.
    2. Run `git diff --stat agent/shared/safety.py` — expect NO changes (frozen floor).
    3. Inspect the diff of agent/shared/models.py — confirm ONLY action_type changed to
       `str`; InfraEvent.domain/severity, HumanEscalationPacket.urgency, and
       estimated_blast_radius remain Literal.
    4. Confirm you accept the deliberate widening: after this change, the only API-level
       guarantee on the diagnoser's action vocabulary is the re-injected per-domain enum
       plus the system-prompt prose; the boundary (threshold + safety_check) is the
       hard backstop that escalates or blocks anything unexpected.
  </how-to-verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_safety.py tests/test_models.py tests/test_orchestrator_routing.py -q` exits 0
    - `git diff --quiet agent/shared/safety.py` exits 0
    - Operator has reviewed the models.py diff and confirmed only action_type changed
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm the safety floor holds and the action_type widening is accepted, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude diagnosis output → action gate | action_type is now an open str; threshold + safety_check are the backstops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering/Elevation | model emits a destructive action_type after D-01 | mitigate | safety_check() blocks all FORBIDDEN_OPERATIONS regardless of domain (safety.py frozen); test_safety asserts drain_node/terminate_instance/drop_table still raise. Blocking human-verify checkpoint. |
| T-01-14 | Tampering | hallucinated/unknown action slips past gating | mitigate | _meets_threshold returns False for unregistered actions; _should_escalate default 0.80 escalates; both asserted with action_type="reboot_node". |
| T-01-15 | Elevation | safety floor accidentally relocated/weakened | mitigate | safety.py is FROZEN; `git diff --quiet agent/shared/safety.py` is an acceptance gate AND a checkpoint verification step. |
| T-01-16 | Tampering | learning_mode bypass under open field | accept | remediate() gate order unchanged (plan 01-02 left body byte-identical except threshold call); full suite re-run confirms learning_mode_blocked path intact. |
</threat_model>

<verification>
- action_type is open str; model schema enum removed; diagnoser re-injects per-domain enum.
- Forbidden ops and learning_mode floor proven intact under the open field.
- safety.py byte-unchanged (git diff gate + checkpoint).
- Full Wave 0 suite green with updated model-schema expectations.
</verification>

<success_criteria>
- D-01/D-02 complete.
- The widening is contained by the existing boundary (threshold + safety_check).
- Human-verify checkpoint approved confirming the safety floor holds.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-and-observers/01-05-SUMMARY.md` when done.
</output>
