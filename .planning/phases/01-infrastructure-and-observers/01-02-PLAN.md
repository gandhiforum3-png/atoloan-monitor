---
phase: 01-infrastructure-and-observers
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - agent/shared/remediation.py
  - agent/skills/remediators/node_remediator.py
  - agent/skills/agents/node_agent.py
  - tests/test_remediation.py
autonomous: true
requirements: [OBS-02]
user_setup: []

must_haves:
  truths:
    - "PreflightResult, _meets_threshold, _log_action, and THRESHOLDS live in agent/shared/remediation.py"
    - "THRESHOLDS is domain-keyed: THRESHOLDS['k8s']['pod_restart'] == 0.80"
    - "_meets_threshold takes a domain argument and looks up THRESHOLDS[domain][action_type]"
    - "node_remediator.py and node_agent.py import these from agent/shared/remediation.py and behave identically (cooldown, PDB, replica floor, learning_mode order all unchanged)"
    - "agent/shared/remediation.py has ZERO kubernetes_asyncio imports"
    - "All Wave 0 baseline tests still pass"
  artifacts:
    - path: "agent/shared/remediation.py"
      provides: "shared remediation primitives: PreflightResult, THRESHOLDS, _meets_threshold, _log_action"
      contains: "THRESHOLDS"
    - path: "agent/skills/remediators/node_remediator.py"
      provides: "K8s-specific preflight/execute bodies + remediate(); imports primitives from shared"
      contains: "from agent.shared.remediation import"
    - path: "agent/skills/agents/node_agent.py"
      provides: "K8s agent tools; imports THRESHOLDS/_meets_threshold/_log_action from shared"
      contains: "from agent.shared.remediation import"
  key_links:
    - from: "agent/skills/remediators/node_remediator.py"
      to: "agent/shared/remediation.py"
      via: "import PreflightResult, THRESHOLDS, _meets_threshold, _log_action"
      pattern: "from agent.shared.remediation import"
    - from: "agent/skills/agents/node_agent.py"
      to: "agent/shared/remediation.py"
      via: "import THRESHOLDS, _meets_threshold, _log_action"
      pattern: "from agent.shared.remediation import"
---

<objective>
Extract the domain-agnostic remediation primitives (D-03/D-04/D-05) out of
node_remediator.py into a new agent/shared/remediation.py, reshaping THRESHOLDS
to a per-domain nested dict. Re-point node_remediator.py and node_agent.py to
import from shared. NO behavior change for the K8s domain.

Lowest-risk extraction; it unblocks every later step. Sequenced first (Wave 1)
per 01-RESEARCH.md's safe-ordering analysis.

Purpose: Make threshold/audit/preflight-result primitives reusable so a future
domain registers `THRESHOLDS["ec2"]["reboot_instance"]` without touching shared code.
Output: agent/shared/remediation.py + re-wired imports, with identical K8s behavior.

Requirement honesty note: Advances the framework groundwork that makes deferred
OBS-03..08 / REM-02..08 small mechanical additions. Delivers nothing new for
those IDs in this plan — only preserves OBS-02 behavior while relocating shared code.
</objective>

<execution_context>
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-and-observers/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-observers/01-RESEARCH.md

<interfaces>
<!-- Exact current shapes being moved. From node_remediator.py (pre-refactor). -->

CURRENT (node_remediator.py lines 31-74) — move OUT to shared/remediation.py:
```python
THRESHOLDS: dict[str, float] = {"pod_restart": 0.80, "deployment_scale_down": 0.85, "human_escalate": 0.00}

@dataclass
class PreflightResult:
    ok: bool
    reason: str = ""

def _meets_threshold(action_type: str, confidence: float) -> bool:
    threshold = THRESHOLDS.get(action_type, 1.0)
    return confidence >= threshold

async def _log_action(redis, incident_id, domain, action, status, confidence, detail) -> None:
    # xadd to "actions:log" with incident_id/domain/action/status/confidence/detail/timestamp
```

CURRENT call sites that must be updated for the domain-keyed shape:
```python
# node_remediator.py:226  -> THRESHOLDS.get(action, 1.0)        becomes THRESHOLDS["k8s"].get(action, 1.0)
# node_remediator.py:48   -> inside _meets_threshold (moves to shared, signature gains domain)
# node_agent.py:331       -> THRESHOLDS[action]                 becomes THRESHOLDS["k8s"][action]
# node_agent.py:375       -> THRESHOLDS[action]                 becomes THRESHOLDS["k8s"][action]
```

STAYS in node_remediator.py (K8s-specific, kubernetes_asyncio-dependent, behavior unchanged):
RESTART_ANNOTATION, RESTART_COOLDOWN_SECONDS, _load_k8s_config, _parse_target,
_check_pdb, _preflight_pod_restart, _preflight_scale_down, _execute_pod_restart,
_execute_scale_down, remediate(...). The remediate() learning-mode order MUST stay:
safety_check -> observe_only -> threshold -> learning_mode -> execute.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent/shared/remediation.py with domain-keyed THRESHOLDS</name>
  <read_first>
    - agent/skills/remediators/node_remediator.py (lines 16-74 — the exact code to move; and lines 209-260 for call-site context)
    - agent/skills/agents/node_agent.py (lines 31-40 imports; 330-333, 374-377 THRESHOLDS uses)
    - agent/shared/event_bus.py (style reference for a shared async-redis module)
    - tests/test_remediation.py (the baseline test to update)
  </read_first>
  <behavior>
    - `THRESHOLDS["k8s"]["pod_restart"] == 0.80`, `THRESHOLDS["k8s"]["deployment_scale_down"] == 0.85`, `THRESHOLDS["k8s"]["human_escalate"] == 0.00`
    - `_meets_threshold("k8s", "pod_restart", 0.80) is True`; `_meets_threshold("k8s", "pod_restart", 0.79) is False`
    - `_meets_threshold("k8s", "deployment_scale_down", 0.85) is True`; `0.84 is False`
    - `_meets_threshold("k8s", "unknown_action", 0.99) is False` (missing action -> default 1.0)
    - `_meets_threshold("unknown_domain", "pod_restart", 0.99) is False` (missing domain -> default 1.0, never crashes)
    - `agent/shared/remediation.py` contains NO `kubernetes_asyncio` import
  </behavior>
  <action>
    Create `agent/shared/remediation.py`. Move into it, verbatim where possible:
    - `PreflightResult` dataclass (ok: bool, reason: str = "").
    - `_log_action(redis: Redis, incident_id: str, domain: str, action: str, status: str,
      confidence: float, detail: str) -> None` — the xadd-to-"actions:log" body, unchanged.
    - `THRESHOLDS`, reshaped to `dict[str, dict[str, float]]`:
      `THRESHOLDS = {"k8s": {"pod_restart": 0.80, "deployment_scale_down": 0.85, "human_escalate": 0.00}}`.
    - `_meets_threshold(domain: str, action_type: str, confidence: float) -> bool`
      implemented as `return confidence >= THRESHOLDS.get(domain, {}).get(action_type, 1.0)`.
      The `.get(domain, {}).get(action, 1.0)` two-level default guarantees unknown
      domain OR unknown action both fall to 1.0 (never met) — preserving the
      "unknown action -> escalate" safety property and never raising KeyError.
    Imports needed: `from dataclasses import dataclass`, `from datetime import datetime, timezone`,
    `from redis.asyncio import Redis`. Do NOT import anything from kubernetes_asyncio.
    Do NOT import structlog unless _log_action used it (it does not — it only xadds).

    Update `tests/test_remediation.py`: change `_meets_threshold` calls to the new
    3-arg `(domain, action, confidence)` signature with `"k8s"` as domain; add the
    two new cases from <behavior> (unknown action with valid domain -> False, unknown
    domain -> False). Import `THRESHOLDS, _meets_threshold, PreflightResult, _log_action`
    from `agent.shared.remediation` instead of node_remediator. Keep `_parse_target`
    imported from `agent.skills.remediators.node_remediator` (it stays there).
    Add assertions: `THRESHOLDS["k8s"]["pod_restart"] == 0.80` and the nested-dict shape.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import agent.shared.remediation" && .venv/bin/python -m pytest tests/test_remediation.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent.shared.remediation"` exits 0
    - `grep -L kubernetes_asyncio agent/shared/remediation.py` prints the filename (i.e. file has NO kubernetes_asyncio import)
    - `grep -q 'THRESHOLDS = {' agent/shared/remediation.py` exits 0
    - `grep -q '"k8s"' agent/shared/remediation.py` exits 0
    - `grep -Eq 'def _meets_threshold\(domain' agent/shared/remediation.py` exits 0 (domain is first param)
    - `grep -q 'class PreflightResult' agent/shared/remediation.py` exits 0
    - `grep -Eq 'async def _log_action' agent/shared/remediation.py` exits 0
    - `.venv/bin/python -m pytest tests/test_remediation.py -q` exits 0
  </acceptance_criteria>
  <done>agent/shared/remediation.py exists with domain-keyed THRESHOLDS, 3-arg _meets_threshold (two-level default), PreflightResult, and _log_action; no kubernetes_asyncio import; updated baseline test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point node_remediator.py and node_agent.py to shared primitives</name>
  <read_first>
    - agent/shared/remediation.py (created in Task 1 — the import source)
    - agent/skills/remediators/node_remediator.py (full file — remove moved defs, fix call sites)
    - agent/skills/agents/node_agent.py (lines 31-40 imports; 330-333, 374-377 THRESHOLDS uses)
    - tests/test_orchestrator_routing.py, tests/test_safety.py (must still pass — they exercise routing/safety unaffected by this move)
  </read_first>
  <action>
    In `agent/skills/remediators/node_remediator.py`:
    - DELETE the local `THRESHOLDS`, `PreflightResult`, `_meets_threshold`, and
      `_log_action` definitions (lines ~31-74) now living in shared.
    - Add `from agent.shared.remediation import PreflightResult, THRESHOLDS, _log_action, _meets_threshold`.
    - Fix the two call sites: in `remediate()`, the `_meets_threshold(action, confidence)`
      call (line ~223) becomes `_meets_threshold("k8s", action, confidence)`; the
      `THRESHOLDS.get(action, 1.0)` in the threshold_not_met detail string (line ~226)
      becomes `THRESHOLDS["k8s"].get(action, 1.0)`.
    - Leave everything else byte-identical: RESTART_ANNOTATION, RESTART_COOLDOWN_SECONDS,
      _load_k8s_config, _parse_target, _check_pdb, _preflight_pod_restart,
      _preflight_scale_down, _execute_pod_restart, _execute_scale_down, and the full
      remediate() control flow (safety_check -> observe_only -> threshold ->
      learning_mode -> target parse -> preflight -> execute). The `safety_check`
      import from agent.shared.safety stays.

    In `agent/skills/agents/node_agent.py`:
    - Change the import block (lines ~31-40): pull `THRESHOLDS, _log_action,
      _meets_threshold` from `agent.shared.remediation`; keep `_execute_pod_restart,
      _execute_scale_down, _load_k8s_config, _preflight_pod_restart,
      _preflight_scale_down` imported from `agent.skills.remediators.node_remediator`.
    - Fix the `_meets_threshold(action, confidence)` calls (lines ~330, ~374) to
      `_meets_threshold("k8s", action, confidence)`.
    - Fix the `THRESHOLDS[action]` references in the threshold_not_met detail strings
      (lines ~331, ~375) to `THRESHOLDS["k8s"][action]`.
    - Change nothing else: _SYSTEM_PROMPT, TOOLS, _format_bundle, all _tool_* bodies,
      _dispatch_tool, _escalate_unresolved, run_incident loop all unchanged.

    Run the full baseline suite to prove equivalence.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import agent.skills.remediators.node_remediator, agent.skills.agents.node_agent" && .venv/bin/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent.skills.remediators.node_remediator, agent.skills.agents.node_agent"` exits 0
    - `grep -q "from agent.shared.remediation import" agent/skills/remediators/node_remediator.py` exits 0
    - `grep -q "from agent.shared.remediation import" agent/skills/agents/node_agent.py` exits 0
    - `grep -cE '^THRESHOLDS|^class PreflightResult|^def _meets_threshold|^async def _log_action' agent/skills/remediators/node_remediator.py` returns 0 (definitions removed, not redefined locally)
    - `grep -q 'THRESHOLDS\["k8s"\]' agent/skills/remediators/node_remediator.py` exits 0
    - `grep -c 'THRESHOLDS\["k8s"\]' agent/skills/agents/node_agent.py` >= 2
    - `grep -q '_meets_threshold("k8s"' agent/skills/agents/node_agent.py` exits 0
    - `grep -q 'safety_check' agent/skills/remediators/node_remediator.py` exits 0 (safety floor still called)
    - `.venv/bin/python -m pytest tests/ -q` exits 0 (all baselines still green)
  </acceptance_criteria>
  <done>Both consumers import shared primitives; all THRESHOLDS lookups are domain-keyed ("k8s"); no local redefinitions remain; remediate() control flow order preserved; full baseline suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude diagnosis output → remediation gate | action_type/confidence cross into threshold + safety gating (unchanged by this move) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | _meets_threshold reshape (D-04) | mitigate | Two-level `.get(domain,{}).get(action,1.0)` default preserves "unknown -> 1.0 -> never met -> escalate"; baseline tests assert unknown-action AND unknown-domain both return False. |
| T-01-04 | Elevation | kubernetes_asyncio leak into shared/ | mitigate | `grep -L kubernetes_asyncio agent/shared/remediation.py` acceptance gate; only pure primitives move, all K8s API bodies stay in node_remediator.py. |
| T-01-05 | Tampering | learning_mode gate order in remediate() | mitigate | remediate() body left byte-identical except the two THRESHOLDS/_meets_threshold call updates; full baseline suite re-run as gate. |
</threat_model>

<verification>
- `agent/shared/remediation.py` imports cleanly and has no kubernetes_asyncio.
- node_remediator.py and node_agent.py import primitives from shared; all THRESHOLDS
  lookups domain-keyed; no local redefinitions.
- Full Wave 0 baseline suite stays green.
</verification>

<success_criteria>
- D-03/D-04/D-05 complete: primitives moved, THRESHOLDS domain-keyed, consumers re-wired.
- Zero K8s behavior change (baseline suite proves equivalence).
- shared/remediation.py is kubernetes_asyncio-free.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-and-observers/01-02-SUMMARY.md` when done.
</output>
