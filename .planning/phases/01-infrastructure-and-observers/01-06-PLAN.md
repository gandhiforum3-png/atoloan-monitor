---
phase: 01-infrastructure-and-observers
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - scripts/run_local.py
  - agent/shared/backoff.py
  - agent/observers/k8s_node_observer.py
  - .planning/ADDING-A-MONITOR.md
  - .planning/REMEDIATION-APPROACHES.md
  - tests/test_registry.py
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06, OBS-01, OBS-02, OBS-03, OBS-04, OBS-05, OBS-06, OBS-07, OBS-08, OBS-09]
user_setup: []

must_haves:
  truths:
    - "scripts/run_local.py iterates agent.registry.REGISTRY and starts one observer task + one generic_orchestrator.run task per registered domain"
    - "run_local.py accepts --monitors k8s,... (comma list) defaulting to all registered domains"
    - "Running run_local.py with the k8s domain reproduces the current diagnoser-mode pipeline; --agent reproduces the agent-mode pipeline"
    - ".planning/ADDING-A-MONITOR.md documents the exact recipe to add a new domain (observer + diagnoser + remediator + optional agent + one DomainConfig)"
    - ".planning/REMEDIATION-APPROACHES.md references reflect the post-refactor module names"
    - "The K8s node monitor mode=diagnoser and mode=agent paths both still work end-to-end (manual checkpoint)"
  artifacts:
    - path: "scripts/run_local.py"
      provides: "registry-driven TaskGroup launcher with --monitors flag"
      contains: "REGISTRY"
    - path: ".planning/ADDING-A-MONITOR.md"
      provides: "the concrete add-a-domain recipe / checklist for the 7 remaining observers"
      min_lines: 60
  key_links:
    - from: "scripts/run_local.py"
      to: "agent.registry.REGISTRY"
      via: "iterate domains -> create_task(observer) + create_task(generic_orchestrator.run)"
      pattern: "REGISTRY"
    - from: "scripts/run_local.py"
      to: "agent.orchestrators.generic_orchestrator"
      via: "run(redis, client, config, ...)"
      pattern: "generic_orchestrator"
---

<objective>
Generalize the local runner (D-14): iterate the registry to start one observer +
one generic_orchestrator per domain, with a --monitors flag. Write the
ADDING-A-MONITOR.md recipe (D-15). Optionally extract _backoff to
agent/shared/backoff.py (D-16). Update REMEDIATION-APPROACHES.md so its module
references stay accurate. Then a blocking manual checkpoint verifies both K8s
paths still work end-to-end.

This is the integration capstone — it proves a new domain auto-wires by being
registry-driven, and it ships the documented checklist that turns the deferred 7
observers into mechanical additions.

Purpose: Make "add a monitor" a one-DomainConfig operation and document it.
Output: registry-driven run_local.py, ADDING-A-MONITOR.md, updated docs, manual
e2e sign-off.

This plan carries ALL phase requirement IDs in frontmatter to satisfy the
coverage gate. See the explicit Requirement Coverage section below for the honest
delivered-vs-deferred breakdown.
</objective>

<requirement_coverage>
## Requirement Coverage (honest accounting — read before verifying)

This phase is the framework refactor sub-scope of Phase 1 (per 01-CONTEXT.md
`<domain>`/`<deferred>` and 01-RESEARCH.md "Requirement-coverage honesty note").
verify-work MUST NOT flag the deferred IDs below as defects of this phase.

| ID | Status after this phase | Evidence |
|----|--------------------------|----------|
| OBS-02 (K8s node observer) | DELIVERED — migrated onto the framework, behavior-preserving | Wave 0 baselines + manual e2e checkpoint; both mode=diagnoser and mode=agent paths intact |
| OBS-09 (InfraEvent normalization) | DELIVERED — schema preserved; event domain now from config.domain | InfraEvent unchanged in models.py; publish() unchanged; registry test |
| OBS-01 (K8s pod observer) | ENABLED-FOR-LATER (deferred) | Reuses domain="k8s" registry entry; ADDING-A-MONITOR.md recipe |
| OBS-03..OBS-08 (EC2, FastAPI, Postgres, log analyzer, sec-group, secrets) | ENABLED-FOR-LATER (deferred) | Framework + ADDING-A-MONITOR.md; open action_type (D-01) lets their actions be expressed |
| INFRA-01..INFRA-06 (EC2 instance, Prometheus, CloudWatch, monitoring Postgres, Redis deploy, heartbeat/watchdog) | DEFERRED — out of scope (deployment/ops, not code refactor) | 01-CONTEXT.md `<deferred>`; future phase |

Framework acceptance test (the validation criterion from 01-CONTEXT.md `<specifics>`):
could an EC2 `reboot_instance` or Postgres `kill_query` action flow through
DomainConfig + generic_orchestrator + shared/remediation + human_escalator without
further core changes? After D-01 (open action_type), D-04 (domain-keyed THRESHOLDS),
D-10..D-13 (registry/generic loop/per-domain urgency), and D-14 (registry-driven
runner) — yes. ADDING-A-MONITOR.md documents exactly that path. No new observers
are implemented here.
</requirement_coverage>

<execution_context>
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-and-observers/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-observers/01-RESEARCH.md
@.planning/REMEDIATION-APPROACHES.md

<interfaces>
<!-- Current runner shape + registry API to wire it to. -->

CURRENT scripts/run_local.py (lines 38-39, 152-163):
  from agent.observers.k8s_node_observer import watch_nodes
  from agent.orchestrators.k8s_orchestrator import run as run_orchestrator   # NO LONGER exists after 01-04
  # hardcoded TaskGroup: watch_nodes(redis), run_orchestrator(redis, client, debounce, learning_mode, mode),
  #   + _tail_events / _tail_escalations / _tail_actions (k8s stream names hardcoded at lines 73, 91)
  # main(verbose, debounce, learning_mode, mode); argparse: --verbose/-v, --debounce/-d, --fix, --agent

POST-REFACTOR registry (from 01-04):
  from agent.registry import REGISTRY            # dict[str, DomainConfig]
  import agent.orchestrators.k8s_orchestrator    # IMPORT triggers register() of the k8s domain
  from agent.orchestrators.generic_orchestrator import run as run_orchestrator
  # DomainConfig fields: domain, stream, consumer_group, observer, diagnose, remediate,
  #   run_incident, escalate_below, urgency_map, context_fetcher
  # observer callable signature: observer(redis, *, verbose=False)  (watch_nodes)
  # generic_orchestrator.run(redis, client, config, *, debounce_seconds, learning_mode, mode)

CURRENT _backoff (k8s_node_observer.py lines 78-81) — D-16 OPTIONAL:
  async def _backoff(attempt: int) -> None:
      delay = min(60.0, (2 ** attempt) + random.uniform(0, 1)); ...; await asyncio.sleep(delay)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make run_local.py registry-driven with a --monitors flag (D-14); optional backoff extract (D-16)</name>
  <read_first>
    - scripts/run_local.py (full file — imports at 38-40, main TaskGroup at 130-167, argparse at 170-182)
    - agent/registry.py (REGISTRY, DomainConfig)
    - agent/orchestrators/k8s_orchestrator.py (must be imported to register the k8s domain)
    - agent/orchestrators/generic_orchestrator.py (run signature)
    - agent/observers/k8s_node_observer.py (watch_nodes signature; _backoff lines 78-81 for the optional D-16 extract)
    - tests/test_registry.py (extend with a --monitors parse helper test if a helper is added)
  </read_first>
  <action>
    Rewrite the launcher portion of scripts/run_local.py to be registry-driven:
    - Replace `from agent.orchestrators.k8s_orchestrator import run as run_orchestrator`
      with `import agent.orchestrators.k8s_orchestrator  # registers the k8s domain`
      and `from agent.orchestrators.generic_orchestrator import run as run_orchestrator`
      and `from agent.registry import REGISTRY`. Remove the direct `watch_nodes` import
      (the observer now comes from `config.observer`).
    - Add a `--monitors` argparse option: `type=str, default=None`, help "comma list of
      domains to run (default: all registered)". Parse into a list; if None, use
      `list(REGISTRY.keys())`. Validate each requested domain exists in REGISTRY; if any
      is unknown, print an error listing available domains and `sys.exit(1)`.
    - In `main(...)`, accept the parsed `monitors: list[str]`. Inside the TaskGroup, loop
      `for domain in monitors:` `cfg = REGISTRY[domain]`; `tg.create_task(cfg.observer(redis,
      verbose=verbose), name=f"{domain}-observer")`; `tg.create_task(run_orchestrator(redis,
      anthropic_client, cfg, debounce_seconds=debounce, learning_mode=learning_mode,
      mode=mode), name=f"{domain}-orchestrator")`. Keep the three tailer tasks; they may
      stay k8s-only (dev convenience) — leave their hardcoded "events:k8s"/"escalations:k8s"
      names as-is for this phase (k8s is the only domain), but add a one-line comment that
      they are k8s-scoped dev tailers.
    - Preserve --verbose/-v, --debounce/-d, --fix, --agent flags and their semantics
      (learning_mode = not args.fix; mode = "agent" if args.agent else "diagnoser").
    - Update the module docstring's task list to reflect "one observer + one
      generic-orchestrator per registered domain".

    D-16 (OPTIONAL — do only if cheap): since no second observer is added this phase,
    DEFER the _backoff extraction by default. If during implementation you are already
    editing k8s_node_observer.py for another reason, you MAY extract `_backoff` to a new
    `agent/shared/backoff.py` (pure: asyncio + random; NO kubernetes_asyncio) and import
    it back into the observer with identical behavior. If you do extract it, add a
    one-line assertion in tests/test_registry.py (or a new tests/test_backoff.py) that
    `from agent.shared.backoff import _backoff` imports. If you defer, note that in the
    SUMMARY and remove agent/shared/backoff.py from files_modified mentally (do not
    create an empty file).

    If you add a `--monitors` parsing helper function, add a small unit test in
    tests/test_registry.py for it (e.g. parse "k8s" -> ["k8s"], parse None -> all keys).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast,sys; ast.parse(open('scripts/run_local.py').read())"</automated>
    <automated>.venv/bin/python scripts/run_local.py --help 2>&1 | grep -- --monitors</automated>
    <automated>.venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import ast; ast.parse(open('scripts/run_local.py').read())"` exits 0 (valid syntax)
    - `grep -q "REGISTRY" scripts/run_local.py` exits 0
    - `grep -q "generic_orchestrator" scripts/run_local.py` exits 0
    - `grep -q -- "--monitors" scripts/run_local.py` exits 0
    - `grep -q "cfg.observer" scripts/run_local.py` exits 0
    - `grep -cE "from agent.orchestrators.k8s_orchestrator import run" scripts/run_local.py` returns 0 (old import removed)
    - `.venv/bin/python scripts/run_local.py --help` exits 0 and output contains "--monitors"
    - `.venv/bin/python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>run_local.py iterates REGISTRY to start observer+orchestrator per domain, validates --monitors, preserves all existing flags; --help works; backoff extracted-or-deferred per discretion; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADDING-A-MONITOR.md (D-15) and update REMEDIATION-APPROACHES.md module references</name>
  <read_first>
    - agent/registry.py (DomainConfig field list — the exact registration shape to document)
    - agent/shared/remediation.py, agent/shared/diagnoser_base.py, agent/shared/agent_loop.py (the shared helpers a new domain reuses)
    - agent/skills/diagnosers/node_diagnoser.py, agent/skills/remediators/node_remediator.py, agent/skills/agents/node_agent.py (the per-domain pattern to mirror)
    - agent/observers/k8s_node_observer.py (observer pattern: watch/poll -> _emit -> publish)
    - agent/orchestrators/k8s_orchestrator.py (the shrunk fetcher + registration example)
    - .planning/REMEDIATION-APPROACHES.md (update module names changed by the refactor)
    - .planning/REQUIREMENTS.md (the 7 deferred observers this recipe targets)
  </read_first>
  <action>
    Create `.planning/ADDING-A-MONITOR.md` — a concrete, copy-followable recipe for
    adding a new domain. Include these sections with real file paths and the actual
    DomainConfig field names:
    1. Overview: a new monitor = 1 observer file + 1 diagnoser file + 1 remediator file
       + optional 1 agent file + 1 DomainConfig registration. Name the shared helpers it
       reuses (agent/shared/event_bus.publish, agent/shared/remediation
       {THRESHOLDS,_meets_threshold,_log_action,PreflightResult}, agent/shared/diagnoser_base.diagnose_with_claude,
       agent/shared/agent_loop.run_tool_loop, agent/shared/safety.safety_check).
    2. Step 1 — Observer: create agent/observers/<domain>_observer.py with a
       watch/poll loop building a problem dict and calling
       `publish(redis, InfraEvent(source=..., domain="<domain>", event_type=...,
       severity=..., resource_id=..., timestamp=..., raw_payload=..., labels=...))`.
       Note InfraEvent.domain is a Literal["k8s","db","infra","app"] — a genuinely new
       domain string requires widening that Literal (call this out explicitly as the one
       schema edit a new layer may need). Mention reusing agent/shared/backoff if extracted.
    3. Step 2 — Diagnoser: create agent/skills/diagnosers/<domain>_diagnoser.py with a
       `_SYSTEM_PROMPT`, a `_format_bundle(bundle, context)`, and a thin `diagnose(client,
       bundle, context)` that calls `diagnose_with_claude(client, _SYSTEM_PROMPT, user_text)`.
    4. Step 3 — Remediator: create agent/skills/remediators/<domain>_remediator.py;
       register the domain's actions/thresholds by adding `THRESHOLDS["<domain>"] = {...}`
       (show the EC2 reboot_instance / Postgres kill_query examples from 01-CONTEXT.md
       `<specifics>`); write domain preflight/execute functions; write a `remediate(...)`
       that calls `safety_check` first, then `_meets_threshold("<domain>", action, conf)`,
       then learning_mode gate, then preflight/execute — mirroring node_remediator's order.
    5. Step 4 — Optional agent file: prompt + TOOLS + _dispatch_tool(name,input,ctx) + a
       thin run_incident that sets up domain ctx and calls run_tool_loop(..., domain="<domain>").
    6. Step 5 — Register: in the domain's orchestrator/wiring module, call
       `register(DomainConfig(domain=..., stream="events:<domain>",
       consumer_group="<domain>-orchestrator", observer=..., diagnose=..., remediate=...,
       run_incident=..., context_fetcher=... or None, escalate_below={...},
       urgency_map={...}))`. Note the runner auto-starts it once imported.
    7. Step 6 — Wire imports: ensure the new wiring module is imported (e.g. from
       run_local.py's registration imports) so register() runs at startup.
    8. Checklist (the actual D-15 checklist) and a "what NOT to touch" note
       (agent/shared/safety.py is frozen; the two threshold dicts stay separate).
    Tie it back to the 7 deferred observers (OBS-01, OBS-03..08) by name.

    Update `.planning/REMEDIATION-APPROACHES.md`: find references to the orchestrator/
    diagnoser/remediator/agent module names and update any that changed in the refactor —
    specifically note that the generic consume/debounce loop now lives in
    agent/orchestrators/generic_orchestrator.py (k8s_orchestrator.py keeps only
    _fetch_pods_on_nodes + the k8s DomainConfig registration), and that the shared
    primitives moved to agent/shared/{remediation,diagnoser_base,agent_loop}.py. Both
    mode="diagnoser" and mode="agent" descriptions must remain accurate. Do not rewrite
    the conceptual content — only correct the module/path references.
  </action>
  <verify>
    <automated>test -f .planning/ADDING-A-MONITOR.md && wc -l .planning/ADDING-A-MONITOR.md | awk '{exit ($1>=60)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/ADDING-A-MONITOR.md` exits 0
    - `grep -q "DomainConfig" .planning/ADDING-A-MONITOR.md` exits 0
    - `grep -q "diagnose_with_claude" .planning/ADDING-A-MONITOR.md` exits 0
    - `grep -q "run_tool_loop" .planning/ADDING-A-MONITOR.md` exits 0
    - `grep -q "safety_check" .planning/ADDING-A-MONITOR.md` exits 0
    - `grep -Eq "THRESHOLDS\[" .planning/ADDING-A-MONITOR.md` exits 0
    - `grep -Eq "reboot_instance|kill_query" .planning/ADDING-A-MONITOR.md` exits 0 (framework acceptance examples)
    - `grep -q "OBS-01" .planning/ADDING-A-MONITOR.md` exits 0 (ties to deferred observers)
    - `wc -l < .planning/ADDING-A-MONITOR.md` >= 60
    - `grep -q "generic_orchestrator" .planning/REMEDIATION-APPROACHES.md` exits 0 (module refs updated)
  </acceptance_criteria>
  <done>ADDING-A-MONITOR.md is a complete add-a-domain recipe naming real files, shared helpers, the DomainConfig shape, the EC2/Postgres action examples, and the 7 deferred observers; REMEDIATION-APPROACHES.md module references corrected for the post-refactor layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full framework refactor is complete (D-01..D-16): shared remediation/diagnoser/
    agent-loop modules, the DomainConfig registry, the generic orchestrator, per-domain
    urgency, the open action_type, a registry-driven run_local.py with --monitors, and
    the ADDING-A-MONITOR.md recipe. All pure-logic behavior is pinned by the Wave 0
    pytest suite (green). The remaining proof is the live end-to-end pipeline, which
    needs Redis + minikube + an API key and therefore cannot be auto-verified.
  </what-built>
  <how-to-verify>
    Prereqs (from run_local.py docstring): minikube running; Redis
    (`docker run -d --name atoloan-redis -p 6379:6379 redis:7-alpine`);
    `export ANTHROPIC_API_KEY=sk-ant-...`; `pip install -r requirements-monitor.txt`.

    1. mode=diagnoser: run `.venv/bin/python scripts/run_local.py` and in another shell
       inject a node-pressure event via `.venv/bin/python scripts/simulate_node_pressure.py`.
       Confirm the OBSERVER -> debounce -> diagnosis -> ESCALATION or ACTION
       (learning_mode_blocked) flow prints exactly as it did before the refactor, and
       that the stream is still events:k8s / escalations:k8s.
    2. mode=agent: run `.venv/bin/python scripts/run_local.py --agent`, inject the same
       event, and confirm the agentic tool-use loop reaches finish_incident or
       human_escalator.escalate as before (learning_mode still blocks mutations).
    3. (Optional) run `.venv/bin/python scripts/run_local.py --monitors k8s` and confirm
       identical behavior to the default (proves the registry path).
    4. Confirm a node_deleted / NetworkUnavailable injection still escalates with the
       same urgency (p3 fall-through) as before.
  </how-to-verify>
  <acceptance_criteria>
    - mode=diagnoser pipeline completes (observe -> diagnose -> escalate/learning_mode_blocked) with events on events:k8s
    - mode=agent pipeline reaches finish_incident or escalation; no mutations in learning_mode
    - NetworkUnavailable/node_deleted path escalates as before (behavior-preserving)
    - Operator confirms behavior is unchanged from the pre-refactor monitor
  </acceptance_criteria>
  <resume-signal>Type "approved" if both K8s paths behave identically to pre-refactor, or describe the divergence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → runner | --monitors selects which domains start; validated against REGISTRY |
| live cluster/Redis → pipeline | manual e2e exercises the real K8s/Redis path (checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Tampering | unknown --monitors domain silently no-ops | mitigate | run_local validates each requested domain against REGISTRY and sys.exit(1) with available list on mismatch. |
| T-01-18 | Spoofing | runner starts wrong stream/group, stranding offsets | mitigate | observer + orchestrator both come from the same DomainConfig (stream/group asserted in test_registry); manual e2e confirms events:k8s unchanged. |
| T-01-19 | Tampering | doc drift (REMEDIATION-APPROACHES.md stale module names) | mitigate | Task 2 updates module references; acceptance grep for generic_orchestrator. |
</threat_model>

<verification>
- run_local.py is registry-driven, validates --monitors, preserves all flags, --help works.
- ADDING-A-MONITOR.md is a complete recipe; REMEDIATION-APPROACHES.md references corrected.
- Full Wave 0 suite green; manual e2e checkpoint confirms both K8s paths unchanged.
</verification>

<success_criteria>
- D-14/D-15 complete; D-16 done-or-deferred per discretion.
- Framework acceptance criterion documented (EC2/Postgres action could flow through unchanged core).
- Manual e2e checkpoint approved: mode=diagnoser and mode=agent behave identically to pre-refactor.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-and-observers/01-06-SUMMARY.md` when done.
</output>
