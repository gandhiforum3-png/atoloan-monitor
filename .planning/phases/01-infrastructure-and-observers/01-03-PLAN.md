---
phase: 01-infrastructure-and-observers
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - agent/shared/diagnoser_base.py
  - agent/shared/agent_loop.py
  - agent/skills/diagnosers/node_diagnoser.py
  - agent/skills/agents/node_agent.py
  - tests/test_imports.py
autonomous: true
requirements: [OBS-02]
user_setup: []

must_haves:
  truths:
    - "agent/shared/diagnoser_base.py exposes diagnose_with_claude(client, system_prompt, user_text, result_model=DiagnosisResult)"
    - "agent/shared/agent_loop.py exposes run_tool_loop(anthropic_client, redis, bundle, incident_id, *, system_prompt, tools, dispatch_tool, format_bundle, learning_mode, max_iterations, domain, ctx_factory)"
    - "node_diagnoser.diagnose keeps only _SYSTEM_PROMPT + _format_bundle and delegates the Claude call to diagnose_with_claude"
    - "node_agent.run_incident keeps only K8s ctx setup + domain pieces and delegates the loop to run_tool_loop"
    - "agent/shared/diagnoser_base.py and agent/shared/agent_loop.py have ZERO kubernetes_asyncio imports"
    - "auto-escalation in the loop is not hardcoded to domain='k8s' — it receives domain"
    - "All Wave 0 baseline tests still pass plus a new import-integrity test"
  artifacts:
    - path: "agent/shared/diagnoser_base.py"
      provides: "generic Claude one-shot diagnosis call (cached prompt, forced tool_choice, parse)"
      contains: "def diagnose_with_claude"
    - path: "agent/shared/agent_loop.py"
      provides: "generic agentic tool-use loop (iterate, dispatch, finish_incident, auto-escalate)"
      contains: "def run_tool_loop"
    - path: "tests/test_imports.py"
      provides: "import integrity + no kubernetes_asyncio in agent/shared/*"
      min_lines: 15
  key_links:
    - from: "agent/skills/diagnosers/node_diagnoser.py"
      to: "agent/shared/diagnoser_base.py"
      via: "diagnose_with_claude call"
      pattern: "from agent.shared.diagnoser_base import"
    - from: "agent/skills/agents/node_agent.py"
      to: "agent/shared/agent_loop.py"
      via: "run_tool_loop call"
      pattern: "from agent.shared.agent_loop import"
---

<objective>
Extract the two Claude-driving control flows into shared modules:
- D-08/D-09: the cached-prompt + forced-tool_choice + parse boilerplate from
  node_diagnoser.diagnose -> agent/shared/diagnoser_base.py::diagnose_with_claude.
- D-06/D-07: the iteration loop from node_agent.run_incident ->
  agent/shared/agent_loop.py::run_tool_loop, leaving K8s ctx/tools/dispatch in node_agent.

Both shared modules must be kubernetes_asyncio-free. K8s behavior identical
(both mode="diagnoser" and mode="agent" paths).

Purpose: A new domain writes only a system prompt + bundle formatter (diagnoser)
or prompt + TOOLS + dispatch (agent); the Claude mechanics come from shared.
Output: diagnoser_base.py + agent_loop.py + slimmed node_diagnoser/node_agent.

Requirement honesty note: Framework groundwork only. Preserves OBS-02; delivers
nothing new for deferred OBS/REM IDs.
</objective>

<execution_context>
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Applications/Atoloan/atoloan-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-and-observers/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-observers/01-RESEARCH.md

<interfaces>
<!-- Exact current shapes being extracted. -->

CURRENT node_diagnoser.diagnose (lines 121-161) — the boilerplate to extract:
```python
response = await client.messages.create(
    model="claude-sonnet-4-6", max_tokens=2048,
    system=[{"type":"text","text":<system_prompt>,"cache_control":{"type":"ephemeral"}}],
    messages=[{"role":"user","content":user_text}],
    tools=[{"name":"submit_diagnosis","description":"...","input_schema": DiagnosisResult.model_json_schema()}],
    tool_choice={"type":"tool","name":"submit_diagnosis"},
)
# token-usage logging; then iterate response.content for tool_use named submit_diagnosis ->
# DiagnosisResult.model_validate(block.input); raise if none found.
```

CURRENT node_agent.run_incident loop (lines 516-603) — the control flow to extract:
```python
messages = [{"role":"user","content": _format_bundle(bundle)}]
# K8s-SPECIFIC setup (STAYS in node_agent): _load_k8s_config(); async with client.ApiClient() as api:
#   ctx = {redis, incident_id, learning_mode, signals, core_v1, apps_v1, policy_v1}
for iteration in range(1, MAX_ITERATIONS+1):
    response = await anthropic_client.messages.create(model="claude-sonnet-4-6", max_tokens=2048,
        system=[{...cache...}], messages=messages, tools=TOOLS)
    messages.append({"role":"assistant","content":response.content})
    # per tool_use block: finish_incident -> capture + ack "acknowledged";
    #   else dispatch_tool(name, input, ctx) -> tool_result json
    # if finish -> _log_action(redis, incident_id, domain, "agent_finish", outcome, 0.0, summary); return outcome
    # if no tool_results -> break (text-only)
    # else messages.append({"role":"user","content":tool_results})
# post-loop: _escalate_unresolved -> human_escalator.escalate(domain=...); return "max_iterations_reached"
```

CURRENT _escalate_unresolved (node_agent 478-490) and _tool_escalate_human (433-459)
both hardcode `domain="k8s"`. The loop's auto-escalation must receive `domain`.

Available shared primitives (from plan 01-02): `_log_action`, `THRESHOLDS`,
`_meets_threshold` in agent/shared/remediation.py. `human_escalator.escalate` stays
in agent/skills/remediators/human_escalator.py (signature: escalate(redis, domain, diagnosis, signals, why)).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract diagnose_with_claude into agent/shared/diagnoser_base.py</name>
  <read_first>
    - agent/skills/diagnosers/node_diagnoser.py (full file — lines 121-161 move; 19-98 stay)
    - agent/shared/models.py (DiagnosisResult, SignalBundle)
    - tests/test_imports.py (will be created here)
  </read_first>
  <behavior>
    - `agent.shared.diagnoser_base` imports cleanly and exposes `diagnose_with_claude`
    - `agent/shared/diagnoser_base.py` contains NO `kubernetes_asyncio` import
    - `node_diagnoser.diagnose` retains `_SYSTEM_PROMPT` and `_format_bundle`
    - The diagnoser tool still uses `result_model.model_json_schema()` as input_schema (so D-01's later enum change flows through automatically)
  </behavior>
  <action>
    Create `agent/shared/diagnoser_base.py` with:
    `async def diagnose_with_claude(client: AsyncAnthropic, system_prompt: str,
    user_text: str, *, result_model: type[BaseModel] = DiagnosisResult,
    model: str = "claude-sonnet-4-6", max_tokens: int = 2048,
    tool_name: str = "submit_diagnosis",
    tool_description: str = "Submit the completed RCA diagnosis") -> BaseModel:`.
    Body = the extracted boilerplate verbatim: messages.create with the system
    prompt wrapped as `[{"type":"text","text":system_prompt,"cache_control":{"type":"ephemeral"}}]`,
    `messages=[{"role":"user","content":user_text}]`, tools=[{name:tool_name,
    description:tool_description, input_schema: result_model.model_json_schema()}],
    tool_choice={"type":"tool","name":tool_name}; the token-usage structlog line;
    iterate response.content for a tool_use block named tool_name ->
    `result_model.model_validate(block.input)` and return it; raise RuntimeError
    if no matching tool_use. Imports: anthropic AsyncAnthropic, structlog, pydantic
    BaseModel, and `from agent.shared.models import DiagnosisResult`. NO kubernetes_asyncio.

    Rewrite `agent/skills/diagnosers/node_diagnoser.py::diagnose` to a thin delegate:
    keep `_SYSTEM_PROMPT` (19-62) and `_format_bundle` (65-98) exactly as-is; the
    new `diagnose(client, bundle, pods_on_node)` builds `user_text = _format_bundle(bundle, pods_on_node)`,
    logs the existing "diagnoser_start" line, then `return await diagnose_with_claude(
    client, _SYSTEM_PROMPT, user_text)`. Add `from agent.shared.diagnoser_base import
    diagnose_with_claude`. Remove the now-moved messages.create/parse code from this file.
    Keep the return type DiagnosisResult.

    Create `tests/test_imports.py`: assert every new/refactored shared module imports
    (`importlib.import_module` for agent.shared.diagnoser_base, agent.shared.remediation,
    agent.shared.models, agent.shared.safety, agent.shared.event_bus); and a test that
    reads each file in `agent/shared/` and asserts `"kubernetes_asyncio" not in <text>`
    using glob over `agent/shared/*.py`. (agent_loop.py is added in Task 2; include it
    in the glob so the same test covers it once it exists.)
  </action>
  <verify>
    <automated>.venv/bin/python -c "import agent.shared.diagnoser_base, agent.skills.diagnosers.node_diagnoser" && .venv/bin/python -m pytest tests/test_imports.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent.shared.diagnoser_base, agent.skills.diagnosers.node_diagnoser"` exits 0
    - `grep -q "def diagnose_with_claude" agent/shared/diagnoser_base.py` exits 0
    - `grep -q "result_model.model_json_schema()" agent/shared/diagnoser_base.py` exits 0
    - `grep -L kubernetes_asyncio agent/shared/diagnoser_base.py` prints the filename (no K8s import)
    - `grep -q "from agent.shared.diagnoser_base import" agent/skills/diagnosers/node_diagnoser.py` exits 0
    - `grep -q "_SYSTEM_PROMPT" agent/skills/diagnosers/node_diagnoser.py` exits 0 (prompt stays)
    - `grep -q "def _format_bundle" agent/skills/diagnosers/node_diagnoser.py` exits 0 (formatter stays)
    - `grep -cE "tool_choice=\{" agent/skills/diagnosers/node_diagnoser.py` returns 0 (Claude call moved out)
    - `.venv/bin/python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>diagnose_with_claude extracted to shared (K8s-free, schema-from-result_model); node_diagnoser is a thin delegate keeping prompt+formatter; import-integrity test green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract run_tool_loop into agent/shared/agent_loop.py</name>
  <read_first>
    - agent/skills/agents/node_agent.py (full file — lines 516-603 move; ctx setup 518-528 + all domain pieces stay)
    - agent/shared/diagnoser_base.py (style reference for a shared Claude module)
    - agent/skills/remediators/human_escalator.py (escalate signature for the auto-escalation fallback)
    - agent/shared/models.py (DiagnosisResult, SignalBundle)
    - tests/test_imports.py (the no-kubernetes_asyncio glob must now cover agent_loop.py)
  </read_first>
  <behavior>
    - `agent.shared.agent_loop` imports cleanly and exposes `run_tool_loop`
    - `agent/shared/agent_loop.py` contains NO `kubernetes_asyncio` import
    - The loop calls `dispatch_tool(name, input, ctx)` and never inspects ctx contents
    - Auto-escalation on max-iterations uses the passed `domain`, not a hardcoded "k8s"
    - `node_agent.run_incident` still performs K8s ctx setup (core_v1/apps_v1/policy_v1) and delegates iteration to run_tool_loop
  </behavior>
  <action>
    Create `agent/shared/agent_loop.py` with:
    `async def run_tool_loop(anthropic_client: AsyncAnthropic, redis: Redis,
    bundle: SignalBundle, incident_id: str, *, system_prompt: str, tools: list,
    dispatch_tool: Callable, format_bundle: Callable, ctx: dict, domain: str,
    learning_mode: bool, max_iterations: int = 6, model: str = "claude-sonnet-4-6") -> str:`.
    Body = the extracted loop verbatim: init `messages=[{"role":"user","content":format_bundle(bundle)}]`;
    `for iteration in range(1, max_iterations+1):` messages.create(model, max_tokens=2048,
    system=[{cache}], messages, tools); append assistant content; for each tool_use block:
    if `block.name == "finish_incident"` capture finish_input + append "acknowledged"
    tool_result; else `result = await dispatch_tool(block.name, block.input, ctx)`
    (wrapped in try/except logging "agent_tool_error"), append json tool_result + log
    "agent_tool_call"; if finish_input set -> `_log_action(redis, incident_id, domain,
    "agent_finish", outcome, 0.0, summary)` and return outcome; if no tool_results ->
    break; else append user tool_results. Post-loop: build the unresolved DiagnosisResult
    (action_type="human_escalate", confidence=0.0, requires_human_review=True,
    estimated_blast_radius="service", root_cause/recommended as in current
    _escalate_unresolved) and call `human_escalator.escalate(redis, domain=domain,
    diagnosis=..., signals=bundle.signals, why="agent loop exceeded max iterations
    without resolution")`; return "max_iterations_reached".
    Imports: anthropic AsyncAnthropic, redis.asyncio Redis, json, structlog,
    typing Callable, `from agent.shared.models import DiagnosisResult, SignalBundle`,
    `from agent.shared.remediation import _log_action`, `from agent.skills.remediators
    import human_escalator`. NO kubernetes_asyncio. (Importing human_escalator from
    skills into shared is acceptable — human_escalator is itself K8s-free; verify it
    has no kubernetes_asyncio import, which it does not.)

    Rewrite `agent/skills/agents/node_agent.py::run_incident` to a thin delegate:
    keep `MAX_ITERATIONS`, `_SYSTEM_PROMPT`, `TOOLS`, `_format_bundle`, every `_tool_*`
    function, and `_dispatch_tool` exactly as-is. run_incident keeps the K8s setup —
    `_load_k8s_config()`, `async with client.ApiClient() as api:` building
    `ctx = {redis, incident_id, learning_mode, signals, core_v1, apps_v1, policy_v1}` —
    then `return await run_tool_loop(anthropic_client, redis, bundle, incident_id,
    system_prompt=_SYSTEM_PROMPT, tools=TOOLS, dispatch_tool=_dispatch_tool,
    format_bundle=_format_bundle, ctx=ctx, domain="k8s", learning_mode=learning_mode,
    max_iterations=MAX_ITERATIONS)`. Delete the now-moved loop body and the local
    `_escalate_unresolved` (its logic now lives in the shared loop's post-loop block).
    Add `from agent.shared.agent_loop import run_tool_loop`. Keep the human_escalator
    import (used by _tool_escalate_human, which stays). Note: _format_bundle here takes
    only `(bundle)` — the loop calls `format_bundle(bundle)`, matching.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import agent.shared.agent_loop, agent.skills.agents.node_agent, agent.orchestrators.k8s_orchestrator" && .venv/bin/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent.shared.agent_loop, agent.skills.agents.node_agent"` exits 0
    - `grep -q "def run_tool_loop" agent/shared/agent_loop.py` exits 0
    - `grep -q "dispatch_tool(" agent/shared/agent_loop.py` exits 0
    - `grep -q "domain=domain" agent/shared/agent_loop.py` exits 0 (auto-escalation uses passed domain)
    - `grep -L kubernetes_asyncio agent/shared/agent_loop.py` prints the filename (no K8s import)
    - `grep -L kubernetes_asyncio agent/shared/diagnoser_base.py agent/shared/remediation.py agent/shared/agent_loop.py | wc -l | grep -q 3` (all three shared modules K8s-free)
    - `grep -q "from agent.shared.agent_loop import run_tool_loop" agent/skills/agents/node_agent.py` exits 0
    - `grep -q "run_tool_loop(" agent/skills/agents/node_agent.py` exits 0
    - `grep -q "client.ApiClient()" agent/skills/agents/node_agent.py` exits 0 (K8s ctx setup stays)
    - `grep -cE "for iteration in range" agent/skills/agents/node_agent.py` returns 0 (loop moved out)
    - `.venv/bin/python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>run_tool_loop extracted to shared (K8s-free, dispatch via callable, domain-parameterized auto-escalation); node_agent keeps domain pieces + K8s ctx setup and delegates; all shared modules kubernetes_asyncio-free; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude tool output → dispatch | tool_use blocks cross into dispatch_tool; safety still enforced inside each K8s tool body (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Elevation | kubernetes_asyncio leak while extracting agent_loop | mitigate | Loop only calls dispatch_tool(name,input,ctx) and never inspects ctx; tool bodies + client.ApiClient stay in node_agent. grep -L gate on all three shared modules. |
| T-01-07 | Tampering | auto-escalation pinned to domain="k8s" | mitigate | run_tool_loop receives `domain`; acceptance gate greps `domain=domain`. Preserves correct escalation for future domains. |
| T-01-08 | Tampering | per-tool safety_check accidentally dropped | accept | safety_check lives inside each _tool_* body (stays in node_agent, untouched); test_safety.py + full suite re-run confirm floor intact. |
</threat_model>

<verification>
- diagnoser_base.py and agent_loop.py import cleanly, both kubernetes_asyncio-free.
- node_diagnoser and node_agent are thin delegates retaining only domain pieces.
- Auto-escalation is domain-parameterized.
- Full Wave 0 baseline suite + import-integrity test green.
</verification>

<success_criteria>
- D-06/D-07/D-08/D-09 complete.
- Both shared modules kubernetes_asyncio-free (gate over all of agent/shared/*).
- Zero K8s behavior change (baselines prove equivalence).
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-and-observers/01-03-SUMMARY.md` when done.
</output>
